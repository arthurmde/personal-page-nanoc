---
title: "My Blog"
---

<h1>Blog</h1>

<div class="row blog-posts">
  <% sorted_articles.each do |post| %>
    <div class="col-xs-12 col-md-8 post">
      <hr>
      <h2>
        <%= link_to post[:title], post.path %>
      </h2>
      <p>
        <span class="glyphicon glyphicon-time"></span>
        Postado em <b><%= post[:created_at_verbose] %></b> por <b><%= post[:author] %></b>
      </p>
      <%= get_post_start(post) %>
      <a class="btn btn-primary" href="<%= post.path %>">Leia Mais <span class="glyphicon glyphicon-chevron-right"></span></a>
    </div>
  <% end %>
</div>
